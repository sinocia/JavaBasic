String 不可变：1、String内部值放在长度不可变的char[]中 2、保存在方法区的常量池中

设计思想：
设计原因：字符串的创建和对象的创建一样，消耗大量的时间空间资源，对于基础类型String，必然存在大量使用

设计思想：1、在方法区开辟一个常量池，类似缓存；2、每次创建有则返回引用，无则加入常量池

实现方法：1、用不可变长char[]数组保存字符串，保证全局不可变，不用担心共享冲突
2、字符串常量池中维护一个表，每个唯一字符串存在一个唯一引用，一直引用着常量池中的对象，不被垃圾回收